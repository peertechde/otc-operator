---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: networks.otc.peertech.de
spec:
  group: otc.peertech.de
  names:
    categories:
    - network
    kind: Network
    listKind: NetworkList
    plural: networks
    singular: network
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.cidr
      name: CIDR
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.externalID
      name: ExternalID
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Network is the Schema for the networks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkSpec defines the desired state of Network
            properties:
              cidr:
                description: Cidr is the IPv4 CIDR block for the network (e.g. "192.168.0.0/24")
                type: string
              description:
                description: Description is an optional human-readable description
                  of the network
                maxLength: 255
                type: string
              orphanOnDelete:
                default: false
                description: OrphanOnDelete prevents deletion of the external resource
                  when the CR is deleted
                type: boolean
              providerConfigRef:
                description: ProviderConfigRef references the ProviderConfig to use
                  for authentication
                properties:
                  name:
                    description: Name of the ProviderConfig
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the ProviderConfig
                    type: string
                required:
                - name
                type: object
            required:
            - cidr
            - providerConfigRef
            type: object
          status:
            description: NetworkStatus defines the observed state of Network.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the Network's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              externalID:
                description: ExternalID is the provider's ID for this Network
                type: string
              lastAppliedSpec:
                description: |-
                  LastAppliedSpec caches the spec that was successfully applied to the
                  external resource. It is used to detect changes to immutable fields.
                properties:
                  cidr:
                    description: Cidr is the IPv4 CIDR block for the network (e.g.
                      "192.168.0.0/24")
                    type: string
                  description:
                    description: Description is an optional human-readable description
                      of the network
                    maxLength: 255
                    type: string
                  orphanOnDelete:
                    default: false
                    description: OrphanOnDelete prevents deletion of the external
                      resource when the CR is deleted
                    type: boolean
                  providerConfigRef:
                    description: ProviderConfigRef references the ProviderConfig to
                      use for authentication
                    properties:
                      name:
                        description: Name of the ProviderConfig
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace of the ProviderConfig
                        type: string
                    required:
                    - name
                    type: object
                required:
                - cidr
                - providerConfigRef
                type: object
              lastSyncTime:
                description: LastSyncTime is the timestamp of the last successful
                  sync with the provider
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed Network spec
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
